<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.Views.XStyle01View">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="lblStyle" TargetType="Label">
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
            </Style>


            <Style TargetType="Label">
                <Setter Property="TextColor" Value="Red"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout>
            <Label  Style="{StaticResource lblStyle}" Text="Nome" />
            <Entry Placeholder="Digite seu nome" PlaceholderColor="Blue"/>
            <Button Text="Enviar" BackgroundColor="CadetBlue"/>

            <Label Text="Resultado" Style="{StaticResource lblStyle}"/>
            <Label Text="Padrão"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>